(%ex "1.9")

; exercise 1.9, page 36

(define (inc n) (- n -1))	; non-text
(define (dec n) (- n 1))	; non-text

(define (+ a b)
  (if (= a 0)
      b
      (inc (+ (dec a) b))))

(+ 4 3)				; non-text

(define (+ a b)
  (if (= a 0)
      b
      (+ (dec a) (inc b))))

(+ 4 3)				; non-text
