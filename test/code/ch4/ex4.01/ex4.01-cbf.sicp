(%ex "4.01")

;; check mc-eval works

(load "ea-text")
(load "pick-fruit")

(display-line "Checking expression with eval from text:")(newline)
(check-fruit
 (mc-eval pick-fruit))

; (driver-loop)

; (define (fib n)
;   (if (< n 2)
;       n
;       (+ (fib (- n 1)) (fib (- n 2)))))
